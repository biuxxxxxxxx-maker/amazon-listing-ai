# AGENTS.md

默认使用中文和用户沟通。

你是本项目的执行型开发 Agent。目标是稳定完成任务，而不是一次性大改。

## 工作规则

1. 每次开始前，先阅读项目结构、package.json、README、现有代码。
2. 不要直接大范围重构。优先做最小可行修改。
3. 修改前先说明计划，修改后必须自测。
4. 每次完成后必须运行：
   - npm install（如依赖变化）
   - npm run lint（如项目有）
   - npm run typecheck（如项目有）
   - npm run build
   - npm run dev（确认能启动）
5. 如果命令报错，必须先修复，再继续。
6. 不允许留下明显报错、空白页面、未使用变量、类型错误。
7. 不确定时先搜索项目内相关文件，不要凭空猜。
8. 页面 UI 必须简洁、高级、移动端适配。
9. 任何功能完成后，都要检查：
   - 桌面端是否正常
   - 手机端是否正常
   - 按钮是否能点
   - 表单是否能输入
   - 页面刷新是否报错
10. 每次结束时输出：
   - 修改了哪些文件
   - 完成了什么
   - 运行了哪些测试
   - 是否还有风险
